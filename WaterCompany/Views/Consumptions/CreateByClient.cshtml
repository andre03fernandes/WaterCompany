@model WaterCompany.Models.ConsumptionViewModel

@{
    ViewData["Title"] = "CreateByClient";
}
<br />
<br />
<h1 style="text-align:center;" class="big-title1"><i class="fas fa-folder-plus"></i> New Consumption</h1>
<hr class="center-ball" />
<br />
<div class="row centerform">
    <div class="col-md-12">
        <form class="form123" asp-action="CreateByClient" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="card text-black" style="border-radius: 5px; box-shadow: rgba(0,0,0,0.35) 0 5px 25px;">
                <div class="card-body">
                    <br />
                    <div class="form-group">
                        <label asp-for="ClientId" class="control-label"></label>
                        <select asp-for="ClientId" asp-items="Model.Clients" class=" form-control text-center" disabled="true"></select>
                        <span asp-validation-for="ClientId" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="ConsumptionDate" class="control-label"></label>
                        <div class="input-group date" data-provide="datepicker">
                            <input type="text" asp-for="ConsumptionDate" class="form-control">
                            <div class="input-group-append">
                                <span class="input-group-text"><i class="fas fa-calendar-alt"></i></span>
                            </div>
                            <span asp-validation-for="ConsumptionDate" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="form-group">
                        <label asp-for="Echelon" class="control-label"></label>
                        <input asp-for="Echelon" class="form-control" />
                        <span asp-validation-for="Echelon" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <br />
                        <button type="submit" value="Save" class="btn btn-outline-primary"><i class="fas fa-save"></i> Save Changes</button>
                        <a class="btn btn-outline-info" asp-action="ClientConsumptions"><i class="fas fa-backward"></i> Back to List</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
<br />
<br />
<br />

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");

        <script type="text/javascript">
            $(document).ready(function () {
                var userLang = navigator.language || navigator.userLanguage;
                console.log(userLang);
                var options = $.extend({}, // empty object
                    $.datepicker.regional[pt - Pt], {
                    dateFormat: "dd/mm/yyyy"
                } // your custom options
                );
                $(".date").datepicker(options);
            });
        </script>
    }
}